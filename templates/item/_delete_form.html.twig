<form method="post" action="{{ path('app_item_delete', {'idItem': item.idItem}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.idItem) }}">
    <button class="btn">Delete</button>
</form>
