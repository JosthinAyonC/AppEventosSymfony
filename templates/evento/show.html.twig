{% extends 'base.html.twig' %}

{% block title %}Evento{% endblock %}

{% block body %}
    <h1>Evento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEvento</th>
                <td>{{ evento.idEvento }}</td>
            </tr>
            <tr>
                <th>CostoEvento</th>
                <td>{{ evento.costoEvento }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ evento.fecha ? evento.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evento_index') }}">back to list</a>

    <a href="{{ path('app_evento_edit', {'idEvento': evento.idEvento}) }}">edit</a>

    {{ include('evento/_delete_form.html.twig') }}
{% endblock %}
